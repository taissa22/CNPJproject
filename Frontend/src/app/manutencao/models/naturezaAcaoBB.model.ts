export class NaturezaAcaoBB {

  constructor(id: number, codigo: number, nome: string) {
    this.id = id;
    this.codigo = codigo;
    this.nome = nome;
  }

  id: number;
  codigo: number;
  nome: string;
}

