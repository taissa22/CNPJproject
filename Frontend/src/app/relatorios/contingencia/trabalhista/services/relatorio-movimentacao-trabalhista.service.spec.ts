/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RelatorioMovimentacaoTrabalhistaService } from './relatorio-movimentacao-trabalhista.service';

describe('Service: RelatorioMovimentacaoTrabalhista', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RelatorioMovimentacaoTrabalhistaService]
    });
  });

  it('should ...', inject([RelatorioMovimentacaoTrabalhistaService], (service: RelatorioMovimentacaoTrabalhistaService) => {
    expect(service).toBeTruthy();
  }));
});
